# P31-003 Internal Scheduler & Job Registry Report

Adds job registry loading, single-instance lock, idempotency-aware scheduler tick, heartbeat writing, and optional job execution. `make scheduler-once` runs one safe dry-run tick.
