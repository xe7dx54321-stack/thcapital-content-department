# P31-012 OpenClaw Scheduling Coexistence Guard Report

Adds a read-only OpenClaw coexistence report that detects overlapping legacy jobs and recommends manual actions. It does not modify `jobs.json`, disable OpenClaw, or call the OpenClaw gateway.
