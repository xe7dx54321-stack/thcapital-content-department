# P31-009 Runtime Control API / CLI Report

Adds `scripts/runtime_control.py` with local-only status, jobs, runs, run, retry, cancel, pause, resume, and shutdown actions. Control commands do not expose external network endpoints and cannot trigger automatic publishing.
