# P31-007 Missed-run Recovery & Catch-up Report

Adds missed-run detection over the configured catch-up window and compresses stale acquisition slots into a consolidated recovery plan instead of mechanically replaying every missed run.
