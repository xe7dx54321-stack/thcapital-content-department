# P31-011 macOS launchd Bootstrap Report

Adds a user-level LaunchAgent plist template plus install, uninstall, and check scripts. The template only launches the autonomous runtime and does not contain secrets or business schedules.
