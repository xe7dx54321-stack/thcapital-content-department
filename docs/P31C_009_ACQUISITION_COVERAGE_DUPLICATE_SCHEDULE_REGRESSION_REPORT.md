# P31C-009 Acquisition Coverage & Duplicate Schedule Regression Report

The acquisition regression gate checks high-value OpenClaw playbook mapping coverage, active lane completeness, weak-signal boundaries, forbidden fetch methods, duplicate source slots, duplicate connector runs, and schedule conflicts.

Blocking failures prevent treating the new cadence as ready.
