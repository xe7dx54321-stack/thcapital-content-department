# 阿里通义GUI-Agent：装上「世界模型」

> 整理日期：2026-03-09
> 标签：#阿里 #通义 #GUIAgent #Mobile-Agent #Agent #AI

---

## 核心内容

### 1. 背景与挑战

"伴随多模态大模型的发展，GUI Agent正成为人机交互的新范式。"

"但在实际生产环境中，构建一个高可用、跨平台的GUI Agent面临诸多工程与算法挑战。"

**三大挑战**：
"真实环境充斥着验证码与异常弹窗导致长轨迹数据极难收集。不同平台如手机、桌面、浏览器的动作空间存在显著差异，混合训练容易引发梯度冲突。同时，真实任务通常需要模型具备长程记忆、工具调用及多Agent协作能力。"

### 2. 解决方案

"为了解决原生GUI模型在端到端落地中的技术壁垒，阿里巴巴通义实验室开源了新一代多平台GUI Agent框架Mobile-Agent-v3.5，并同步发布了其背后的原生基座模型家族GUI-Owl-1.5。"

"该模型家族在20多个主流GUI Benchmark上取得了开源领域领先的测试结果，实现了跨桌面、手机、浏览器等多端的统一控制。"

"项目开放了从2B到32B的多种参数尺寸，并针对实际部署需求解耦出Instruct和Thinking两种模型变体，以支持从端侧极速响应到云端复杂推理的全链路应用。"

### 3. 架构设计：解耦执行与推理

"Instruct模型被设计为专注极速响应与轻量化执行，它不输出思考链，直接基于当前上下文给出动作决策，非常适合部署在算力受限或隐私要求较高的端侧设备上处理高频实时交互。"

"Thinking模型则专攻长程复杂任务，具备完整的思维链能力，能够进行详尽的规划、反思与纠错。"

"云端的大参数Thinking模型可作为规划者拆解复杂任务，端侧Instruct模型作为执行者完成原子操作，从而构建出高效的多Agent协作系统。"

**外部工具调用**：
"Mobile-Agent-v3.5在动作空间上原生支持了外部工具调用与MCP（Model Context Protocol）协议。Agent在遇到复杂计算或数据库查询时，可以直接调用API完成工作流闭环。"

### 4. 核心技术原理

#### 4.1 混合数据管线解决长轨迹合成难题

"高质量的Grounding（视觉定位）和长轨迹数据是训练GUI模型的关键。研发团队放弃了单纯依赖真实设备硬采的低效路线，构建了一套混合数据生产管线。"

**Grounding数据**：
"针对专业软件和复杂多窗口的高难度场景，团队利用多模态模型结合参考界面进行多轮校验，直接合成高分辨率的复杂UI截图。"

"为了实现规模化扩展，算法通过轨迹挖掘和App教程字幕解析抽取海量交互问答。"

"团队还特别引入了大规模的Infeasible Query（不可行查询）负样本，通过随机配对并经过多模型共识过滤，让模型学会在找不到目标控件时主动拒绝操作。"

**轨迹数据**：
"研究团队引入了基于有向无环图（DAG）的任务合成机制。真实环境中动辄出现报错和反爬弹窗，为此系统设计了截断与任务修复机制。"

"当Agent在某一步失败时，系统会自动将轨迹回溯并截断到上一个正确的检查点，随后重写任务指令，将部分正确的轨迹转化为高质量的前缀监督数据。"

"对于充满验证码的高频场景，团队基于Web渲染构建了大量虚拟环境，通过精准的环境反馈低成本生成了海量专家轨迹。"

#### 4.2 世界模型注入与统一思维链合成

"一个工程可用的Agent必须具备上下文记忆与状态预测能力。模型在训练阶段通过抓取全网软件官方文档和问答论坛数据掌握了软件常识。"

"同时，研发团队利用轨迹记录训练模型进行状态转移预测，要求模型在执行动作前预判下一屏的UI会发生什么变化（例如焦点是否转移、弹窗是否出现），以此降低长程决策的试错成本。"

**统一CoT流水线**：
"更核心的改造在于统一的CoT合成流水线。收集到的所有多端轨迹数据都被进行了思维扩充。在每一步动作发生前，模型被要求输出结构化的中间状态，包括："

"- 观察当前屏幕的关键信息"

"- 提取后续任务需要的记忆数值"

"- 反思上一步的操作结果是否符合预期并进行纠错"

"- 最后综合这些信息与外部工具定义来决策具体的GUI动作"

"这种训练方式让模型获得了优秀的全局任务规划能力。"

#### 4.3 MRPO算法解决多平台强化学习瓶颈

"在强化学习阶段，试图用单一策略同时控制手机、PC和浏览器极易引发梯度冲突与训练崩溃。为此，团队提出了MRPO（Multi-platform Reinforcement Policy Optimization）算法。"

**解决结果坍塌问题**：
"GUI任务的容错率极低，这给基于GRPO的分组强化学习带来了独特挑战。在标准GRPO中，系统会为每个任务采样一组n条轨迹，通过组内轨迹的结果差异来计算优势函数。然而在GUI场景下，这n条轨迹常常会全部成功或全部失败，导致结果坍塌，组内方差为零，优势估计失效。"

"团队设计的在线过采样机制巧妙地在保持On-policy前提下解决了这一问题。系统先为任务从当前策略超量采样kn条轨迹构成候选池，随后从这个候选池中均匀随机下采样n条轨迹组成实际训练组。"

"关键在于后续的条件替换策略：若下采样得到的训练组仍然所有轨迹结果相同而坍塌，但池中存在异质样本，系统会随机替换训练组中的一条轨迹为池中结果相反的轨迹。"

"这套机制在不引入Off-policy偏差的前提下，对于成功率为p的任务，将非坍塌训练组的出现概率大幅提升，显著提升了训练效率。"

---

## 模型规格

| 参数 | 规格 |
|-----|------|
| 参数尺寸 | 2B到32B多种选择 |
| 模型变体 | Instruct（端侧）和Thinking（云端） |
| 支持平台 | 桌面、手机、浏览器多端统一 |
| Benchmark | 20+主流GUI Benchmark开源领先 |

---

## 投资启示

1. **GUI-Agent**：人机交互新范式，市场空间巨大
2. **端云协同**：兼顾延迟与推理深度的架构创新
3. **数据挑战**：长轨迹数据是核心瓶颈，混合数据管线是解决方案
4. **阿里布局**：通义在Agent领域持续投入，开源战略
5. **多平台RL**：MRPO算法解决梯度冲突是工程突破

---

*来源：量子位 | 通义千问团队*
