# Redteam Review

- `date`:
- `stage`:
- `owner`: `redteam-reviewer`
- `review_target`:
- `generated_at`:
- `review_posture`: `repair-first`
- `minimum_verification_done`: `yes / no`
- `checked_sources_or_actions`:
- `topic_preservation_judgment`: `keep_and_fix / keep_but_deprioritize / replace_only_if_false`

## 总评

- `结论`:
- `是否建议放行`:
- `最危险问题`:
- `问题类型`: `repairable / fatal / mixed`
- `是否建议直接换题`: `no / yes`
- `默认补救路径`: `补证 / 补覆盖 / 改角度 / 改结构 / 补素材 / 修排版`

## 高优先级问题（必须修）

### P1
- `问题`:
- `问题定性`: `repairable / fatal`
- `为什么严重`:
- `我已经核查了什么`:
- `会伤害什么结果`:
- `优先补救动作`:
- `若补救失败，再考虑什么`:

### P2
- 同上

### P3
- 同上

## 中优先级问题（建议修）

- `问题`:
- `建议`:

## 亮点（避免误杀）

- `值得保留的优点`:
- `为什么不该直接否掉`:

## 优先补救顺序

1. 
2. 
3. 

## 给 `market-editor` 的裁判提示

- `建议评分区间`:
- `建议 rework_mode`: `supplement_evidence / expand_validation / rewrite_angle / rewrite_quality / replace_topic`
- `建议联动岗位`: `signal-scout / topic-planner / content-writer / publish-ops`
- `是否建议保留原对象返工`: `yes / no`
- `低于8分的核心原因`:
- `若放行，需接受的风险`:
- `只有什么情况下才建议换题`:
