# 平台任务单

- `date`:
- `owner`: `topic-planner`
- `generated_at`:
- `input_pack`:
- `stage_gate_status`: `drafting / premium_pass / continuity_only`
- `stage_gate_rule`: `若为 continuity_only limited task sheet，则最多覆盖 3 个最重要平台，每个平台先保 1 个任务槽位，其余平台写入 Holdout`

## 全局主池 Top6

| rank | topic_key | 核心判断 | 为什么值得写 | 主要风险 |
|---|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |
| 4 |  |  |  |  |
| 5 |  |  |  |  |
| 6 |  |  |  |  |

## 六个主战场任务单

### `wechat`
#### Task 1
- `topic_key`:
- `目标读者`:
- `切入角度`:
- `核心论点`:
- `证据抓手`:
- `source_ref_bundle`:
- `视觉建议`:
- `为什么适合该平台`:

#### Task 2
- 同上

### `xiaohongshu`
- 复用同样结构，保留 2 个任务槽位。

### `zhihu`
- 复用同样结构，保留 2 个任务槽位。

### `x`
- 复用同样结构，保留 2 个任务槽位。

### `bilibili`
- 复用同样结构，保留 2 个任务槽位。

### `toutiao`
- 复用同样结构，保留 2 个任务槽位。

## `baijiahao` SEO 镜像层判断

- `是否需要单独立题`:
- `理由`:
- `承接哪篇主稿更优`:

## Holdout 清单

### `<topic_key>`
- `为什么能进最终池`:
- `为什么这轮没选`:
- `什么时候可捞回`:
