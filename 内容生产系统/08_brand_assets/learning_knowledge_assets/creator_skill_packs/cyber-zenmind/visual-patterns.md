# 视觉模式｜赛博禅心

- `generated_at`: `2026-05-09 17:44:21 CST`
- `sample_gate`: `passed (8/20)`
- `median_image_count`: `2.5`
- `median_first_image_after_text_paragraphs`: `11`
- `early_image_share`: `0%`
- `dense_visual_share`: `25%`
- `median_image_density_per_1000_chars`: `0.67`

## 从 20 篇样本里稳定看到的图文节奏

- 如果首图靠后，正文前段就要更强地承担对象识别与证据说明。

## 统一审美的落地要求

- 封面和正文图统一走 `proof-first editorial` 路线：纸白 / 浅灰底，深墨文字，只保留 1 个信号色，不做多色霓虹拼盘。
- 封面优先用 `对象截图 / 数字信号卡 / 结构图`，不再用浮夸 AI 人像、赛博脑回路、悬浮机器人、无对象光束图。
- 正文图固定优先级：`原始证据图 > 解释图 > 节奏换气图`；如果某张图既不能证明事实，也不能解释结构，就删掉。
- 解释图优先做 `四类信号卡 / 分层架构图 / workflow 循环图 / 对比矩阵` 这类结构化卡，而不是“看起来像图”的抽象海报。

## 样本里的代表性图文案例

- `飞书出了官方 CLI，消息、文档、日历、邮箱都能用 Agent 操作了`｜图片 `6` 张｜首图前文字段落 `8`｜场景/自述切入；结构展开后再上图；短段密度高；图文节奏偏密
- `爱马仕：一家做了 189 年 Harness 的公司`｜图片 `2` 张｜首图前文字段落 `11`｜数字/清单切入；结构展开后再上图；短段密度高
- `给 AI 配张银行卡：Stripe 半年搭完的 Agent 支付全景`｜图片 `3` 张｜首图前文字段落 `11`｜对象/事件先落地；结构展开后再上图；短段密度高
- `林俊旸离职后首发长文：从「想得更久」到「为行动而想」`｜图片 `1` 张｜首图前文字段落 `4`｜场景/自述切入；判断后很快补图；短段密度高
- `Google 新算法，Mac Mini 也能跑 Qwen3.5`｜图片 `4` 张｜首图前文字段落 `33`｜事件/变化切入；结构展开后再上图；短段密度高
- `Sora 关停，迪士尼 10 亿美元投资作废`｜图片 `0` 张｜首图前文字段落 `n/a`｜场景/自述切入；短段密度高
- `Token 的译名，不再是量子位`｜图片 `0` 张｜首图前文字段落 `n/a`｜问题/提问切入；短段密度高
- `Agent，能自动做生意、赚真钱了`｜图片 `6` 张｜首图前文字段落 `11`｜场景/自述切入；结构展开后再上图；短段密度高；图文节奏偏密
