# Redteam Review｜day_mainline publish-ready 成品包（第 2 轮）

- `date`: `2026-04-22`
- `stage`: `content-pack｜wechat`
- `owner`: `redteam-reviewer`
- `review_target`: `openai_news_scaling_codex_to_enterprises_worldwide_20260422`
- `generated_at`: `2026-04-22 20:16 CST`
- `review_posture`: `repair-first`
- `minimum_verification_done`: `yes`
- `checked_sources_or_actions`:
  - wechat.md（全文，113行，5895字节，mtime=19:20）
  - 00_draft-pack-card.md（updated_at=19:23，说明 pack 在 19:20 后有更新）
  - title-options.md
  - packaging-bundle.md
  - wechat-html-handoff.md
  - inline-visual-plan.md
  - visual-assets/*.png（共 6 个）
  - visual-assets/_asset-manifest.json
  - visual-assets/_capture-log.md
- `previous_review`: `2026-04-22 18:05（第一轮，审的是 18:05 版本的 wechat.md handoff 骨架）`
- `topic_preservation_judgment`: `keep_and_fix`

---

## 总评

- `结论`: **第 1 轮红队（18:05）发现的所有致命问题已在 19:20 版本全部修复。** wechat.md 现在是一篇 113 行、5,895 字节、结构完整的真实微信稿，无 handoff 残留，正文各段均有实质内容。但新的致命问题仍然存在：**全部 6 张图片文件字节为 0**，视觉资产实际为空，发布级正文配了一张空白图，这在任何平台都是直接拒稿级别的硬伤。另有一个发布风险：标题"# 微信稿｜Scaling Codex：ToB 这步棋，为什么是现在"在公众号信息流里属于第二屏以下层级的小标题，不是读者第一眼看到的主标题，需要确认实际公众号排版后的显示效果。
- `是否建议放行`: **否。图片资产为空是致命阻塞，不具备入草稿箱的技术条件。**
- `最危险问题`: **6 张 visual-assets/*.png 全部字节为 0，正文引用的图片全部是空白文件。**
- `问题类型`: `fatal`（技术层：图片为空；内容层：标题显示位置待确认）
- `是否建议直接换题`: `no`（正文质量已经达标，换题浪费已完成的写作投入）
- `默认补救路径`: **补图或删图引用 + 确认封面标题显示层**

---

## 高优先级问题（必须修）

### P1｜全部 6 张 PNG 图片字节为 0，视觉资产实际为空

- `问题`: 执行 `stat -c %s` 逐一测量 visual-assets/ 目录下全部 6 个 PNG 文件，结果均为 0 字节（00__cover.png、01__openai_com_index_scaling_codex_to_enterprises_wo.png、02__openai_com_news_rss_xml.png、81__slot_2.png、82__slot_3.png、83__slot_4.png）。wechat.md 正文 3 次引用图片（slot_1/slot_2/slot_4），引用的全部是空文件。
- `问题定性`: `fatal`
- `为什么严重`: 微信公众号发布后，读者看到的是空白图片占位符，或者图片加载失败图标。这在任何平台都是立即拒稿级别的技术事故，而且会严重损害品牌可信度。
- `会伤害什么结果`: 读者打开文章看到图片全是裂图或空白，可信度归零，收藏率和转发率归零。
- `我已经核查了什么`: 用 `stat -c %s` 逐一测量每个 PNG 文件，全部返回 0。`ls -la` 显示文件存在但内容为空（只有文件名元数据，无实际内容）。
- `优先补救动作`: content-writer 或 publish-ops 必须：
  1. 确认图片生成/抓取过程哪里出了问题（playwright 在该环境有已知权限问题）
  2. 用 jina reader 直接抓页面内容，或手动截图，或用其他工具重新生成 4 张 slot 图
  3. 如果在 21:30 前无法补回真实图片，必须从 wechat.md 里删除所有 `![]()` 图片引用，改为纯文字版本发布
  4. 不能带着 6 张空白图片入草稿箱
- `若补救失败，再考虑什么`: 如果 21:30 前无法恢复图片，content-writer 必须把 wechat.md 里的所有 `![]()` 图片引用行删掉，让文章以纯文字形式入草稿箱。这比带着空白图发布更体面。

---

### P2｜标题实际显示位置待确认，封面图标题层缺失

- `问题`: wechat.md 的文件内标题是 `# 微信稿｜Scaling Codex：ToB 这步棋，为什么是现在`。这个 `#` 在 markdown 里是一级标题，但在公众号排版流程中，这个 "# 微信稿｜" 前缀在最终发布后是否显示、显示在哪里，需要确认。如果这个被当成副标题或正文小标题处理，读者在信息流第一屏看不到任何有效标题。
- `问题定性`: `fatal`（发布风险）
- `为什么严重`: 公众号信息流里，读者第一眼看到的是封面标题，不是正文第一行。如果封面标题和正文标题不一致，或者正文第一行被当成次要信息处理，打开率会受影响。
- `会伤害什么结果`: 信息流露出时标题不清晰，第一关打开率塌陷。
- `我已经核查了什么`: wechat-html-handoff.md 的 formatting guidance 没有明确说封面标题从哪取、公众号平台如何映射 markdown 标题层级。
- `优先补救动作`: content-writer 或 publish-ops 必须确认：1）wechat.md 里的 `#` 标题是否对应公众号封面大标题；2）如果"微信稿｜"前缀会被公众号平台当成正文处理，需要在 wechat-html-handoff.md 里写清楚封面标题实际要用的文本。如果封面图本身有标题文字层，封面文字和正文标题是否一致。
- `若补救失败，再考虑什么`: 如果无法在 21:30 前确认，至少在飞书云文档里写清楚封面标题实际应显示的文字，供老板人工确认。

---

## 中优先级问题（建议修）

### 中 1｜wechat-html-handoff.md 提到"第一屏必须完成三件事"，但 wechat.md 实际开头是 hook 句而非背景交代

- `问题`: handoff 要求"第一屏必须完成：对象是什么、为什么现在值得看、核心判断是什么"，但 wechat.md 实际开头是 hook 句"很多人看到 OpenAI 推企业级 Codex 的新闻，第一反应是……"。这两段之间没有显式的背景交代。
- `建议`: content-writer 在 hook 句后加一句"先补背景"过渡，让读者明确感知到"这是背景介绍"，而不是直接跳到下一段解释。

### 中 2｜wechat.md 没有"太长不看"摘要块

- `问题`: 按 runbook，双车道日间主线稿应有"太长不看"摘要，但 wechat.md 全篇没有这个结构。
- `建议`: 如果后续还要扩展到知乎或其他平台，补一个"太长不看"块；如果纯公众号不补也可以接受，不强制。

---

## 亮点（避免误杀）

- `值得保留的优点`:
  1. **正文质量已全面达标**：113行、5895字节、无handoff残留、各section均有实质内容（事实+影响+判断）、无内部评分语言泄露、结构完整（01-06六段式）。
  2. **钩子设计合理**：开头用"很多人第一反应是'又一个企业合作公告'"这种反直觉预判开头，然后给出"它回答了一个问题"，有效激活读者好奇心。
  3. **三层展开逻辑清晰**：01事件本身 → 02为什么是现在 → 03对AI/Agent主线的影响，符合"先事实后判断"的黄金结构。
  4. **结尾有真实CTA**：文末"如果你对这个方向有一手观察，欢迎把看到的信号发给我们"，是可发的、有人味的结尾，不是草稿注释。
  5. **title-options.md 有6个可用候选**，任选一个都好过当前用的内部评分语言标题。
  6. **图位设计合理**：slot_1/2/3/4 对应01/02/03/04各段，如果图片能补回来，图文节奏是完整的。
- `为什么不该直接否掉`: 正文已经是一篇 publish-ready 质量的微信稿，不需要重写，只需要解决图片和标题显示两个技术层问题。换题等于把一篇已经写好的稿子扔掉，浪费 content-writer 的劳动。

---

## 优先补救顺序

1. **【致命·立即处理】确认图片状态并补图或删图引用**
   - 当前 6 张图全部 0 字节，必须立即确认是生成失败还是文件损坏
   - 如果是 playwright 权限问题导致截图失败，用 jina reader 或手动截图绕过
   - 如果 21:30 前无法补回真实图片，从 wechat.md 删除所有 `![]()` 图片引用行，以纯文字版入草稿箱
   - 绝对不能带空白图入发布队列

2. **【致命·立即处理】确认公众号封面标题实际显示文本**
   - wechat-html-handoff.md 需要明确写出：公众号封面标题应显示的实际文本是"Scaling Codex：ToB 这步棋，为什么是现在"
   - 如果封面图本身有文字层，确认封面文字与正文标题一致
   - 在飞书云文档备注里写清楚，供老板人工确认

3. **【建议】在 hook 后加一句"先补背景"过渡**
   - 让读者明确感知到接下来是背景介绍，不是正文第一层

---

## 给 `market-editor` 的裁判提示

- `建议评分区间`: **6-7 分**（正文质量好，但图片资产为空是致命技术阻塞；若补图问题解决，可到 8-9 分）
- `建议 rework_mode`: `rewrite_quality`（正文已不需要重写，但图片必须补回或删除引用）
- `建议联动岗位`: `content-writer`（主：补图或删图引用）+ `publish-ops`（辅助：确认封面标题显示）
- `是否建议保留原对象返工`: **yes**（正文已达标，换题浪费已完成工作）
- `低于 8 分的核心原因`: 全部图片字节为 0，技术层致命阻塞，无法进入发布队列
- `若放行，需接受的风险`: 带着空白图发出去，读者看到的是裂图，直接砸品牌
- `只有什么情况下才建议换题`: 如果确认图片无法在 21:30 前以任何方式补回（包括删图引用改纯文字版），且老板决定不接受纯文字版，才考虑换题。当前强烈不建议换题，正文质量已达标。

---

## 返工达标门槛

本次 pack 重新提交时，必须同时满足：

1. ✅ **图片资产真实可用**：visual-assets/ 下的 PNG 文件有真实内容（非 0 字节），或 wechat.md 删除了所有图片引用行改为纯文字版
2. ✅ **封面标题明确**：wechat-html-handoff.md 或飞书云文档里明确写出公众号封面标题实际显示文本
3. ✅ **正文质量维持**：不做任何降低正文质量的修改

不满足第 1 条，不具备复审资格。
