# Stage Gate Scorecard

- `date`: `2026-04-20`
- `stage`: `top20`
- `owner`: `market-editor`
- `delivery_pack`: `/Users/apple/Documents/同行资本内容部门/内容生产系统/03_topic_candidates/20260420__top20-screening-pack.md`
- `redteam_review`: `/Users/apple/Documents/同行资本内容部门/内容生产系统/10_logs/20260420__top20__redteam-review.md`
- `generated_at`: `2026-04-20 15:23:03 CST`

## 裁判结论

- `score`:
- `status`: `pass / rework`
- `status_rule`: `status 只允许写 pass 或 rework；若进入 continuity lane，也仍写 rework，并把 continuity 结果写进下方两个字段`
- `rework_mode`: `supplement_evidence / expand_validation / rewrite_angle / rewrite_quality / replace_topic`（可多项组合，用 `+` 连接）
- `是否保留原对象`: `yes / no`
- `topic_value_judgment`: `高 / 中 / 低`
- `execution_readiness`: `接近通过 / 可补强 / 暂不可发`
- `publish_ready_platforms`: `none / wechat / x / wechat,x ...`（仅当整体仍为 rework，但某个平台可 continuity-only 先行时填写）
- `continuity_decision`: `premium_only / continuity_only / stop_for_truth`
- `continuity_output`: `none / top20_mini_slate / limited_task_sheet / backlog_publish / carry_rework_backlog`
- `continuity_rule`: `Top20 <8 且非 truth failure 时默认写 continuity_only，并优先产出 top20_mini_slate；platform-task <8 且非 truth failure 时默认写 continuity_only，并优先产出 limited_task_sheet；content-pack <8 且非 truth failure 时必须保留最高分对象继续推进，若已有平台可先行则写 publish_ready_platforms 并用 backlog_publish 标记`
- `是否进入下一工序`:

## 评分理由

- `做得好的地方`:
- `当前主要缺口`:
- `为什么是这个分数`:
- `先改什么`:
- `后改什么`:

## 若打回，必须修的三件事

1. 
2. 
3. 

## 返工顺序说明

- `先补证还是先换题`:
- `是否允许补证后原对象复评`: `yes / no`
- `若建议换题，触发条件`:

## 若放行，进入下一步的明确动作

- `next_owner`: `可写多个岗位，用 + 连接`
- `next_output`:
- `deadline_or_expectation`: `必须写实际生效 deadline；若为 day_mainline 的 wechat 草稿箱交付，一律不得晚于当日 19:00 CST；若为 morning_flash 自动发布，一律写当日 06:50 CST`
